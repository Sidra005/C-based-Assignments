//Program to covert the length in feet to centimeter.
#include<stdio.h>
void main()
{
    int Lft;
    scanf("%d",&Lft);
    int Lcm;
    Lcm= (Lft*30);
    printf("\n Calculation of the length in cm by multiplying length in ft with 30=%d",Lcm);

}


